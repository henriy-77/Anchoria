[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "SHARED_SECRET"

[functions]
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"

# On the invest.anchoriaonline.com domain, the IPO onboarding page is the
# homepage -- not the KYC account-type chooser that index.html serves on
# the main netlify.app domain.
[[redirects]]
  from = "/"
  to = "/ipo-onboarding.html"
  status = 200
  force = true
  conditions = {Host = ["invest.anchoriaonline.com"]}

# Send the old form URLs to the current renamed pages
[[redirects]]
  from = "/accountopening.html"
  to = "/individualaccountopening.html"
  status = 301
  force = true

[[redirects]]
  from = "/corporate-opening.html"
  to = "/corporateaccountopening.html"
  status = 301
  force = true


[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/account-opening.html"
  [headers.values]
    # Allow embedding via iframe from the main Anchoria site
    X-Frame-Options = "ALLOWALL"
    Content-Security-Policy = "frame-ancestors *"
